You are APIMart, the metadata-writing provider for a concise English Project P.I.T.T. game guide. Return only one valid JSON object with exactly these fields: title, description, h1, quickAnswer, facts. h1 must equal title. Write public-facing player copy only: do not expose editorial workflow, research-packet language, source labels, confidence labels, or commentary about what the guide does not claim. Use only verified facts from research.md. Keep description under 160 characters and concise. The title should remain “Project P.I.T.T. Anomaly: Recipe, Phase 4 Role, and the Percentage Pipe”. The quickAnswer must directly say 1 Duck + 1 Cash Register + 1 Piñata at the Anomaly workbench makes 1 Anomaly, and that 10 Anomalies fill the Percentage Pipe to 100%. The facts array is mandatory and must contain exactly four objects, each with non-empty label and value. Use these four labels: “Recipe”, “Phase 4”, “Keep inputs supplied”, “Percentage Pipe”. Do not use Markdown links or placeholder tokens in JSON values.
